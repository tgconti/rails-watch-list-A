<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to root_path, class:"navbar-brand" do %>
      <%= image_tag "movie_night_logo.png", alt: "Logo" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <form class="<%= current_page?(lists_path) || current_page?(root_path) ? 'd-flex' : 'd-none' %>">
        <input class="form-control me-2" name="query" type="search" placeholder="Search Lists" aria-label="Search" value="<%= params[:query] %>">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
      <div class="d-flex justify-content-end nav-list-cont">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'My Lists', lists_path, class: "nav-link #{current_page?(lists_path) || current_page?(root_path) ? 'active' : ''} " %>
          </li>
          <li class="nav-item">
            <%= link_to 'New List', new_list_path, class: "nav-link #{current_page?(new_list_path) ? 'active' : ''}" %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
